<UserControl x:Class="ClaimTracker.Windows.ClaimsListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClaimTracker.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BackgroundGrayBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Border Grid.Row="0" Background="{StaticResource WhiteBrush}" 
                BorderBrush="{StaticResource LightGrayBrush}" 
                BorderThickness="0,0,0,1"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                          Style="{StaticResource PageTitle}"
                          Margin="0"
                          Text="Claims Management"/>

                <Button Grid.Column="1"
                       Style="{StaticResource PrimaryButton}"
                       Content="+ Add New Claim"
                       Click="AddNewClaim_Click"
                       Padding="20,10"/>
            </Grid>
        </Border>

        <!-- Claims DataGrid -->
        <DataGrid Grid.Row="1"
                 Name="ClaimsDataGrid"
                 Style="{StaticResource StandardDataGrid}"
                 ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                 Margin="20"
                 IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Claim Number" 
                                   Binding="{Binding ClaimNumber}" 
                                   Width="120"/>
                <DataGridTextColumn Header="Policy Holder" 
                                   Binding="{Binding PolicyHolderName}" 
                                   Width="200"/>
                <DataGridTextColumn Header="Date Filed" 
                                   Binding="{Binding DateFiled, StringFormat='{}{0:MM/dd/yyyy}'}" 
                                   Width="100"/>
                <DataGridTextColumn Header="Amount" 
                                   Binding="{Binding ClaimAmount, StringFormat='{}{0:C}'}" 
                                   Width="100"/>
                <DataGridTextColumn Header="Status" 
                                   Binding="{Binding Status}" 
                                   Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource ActionButton}" 
                                       Content="Edit" 
                                       Click="EditClaim_Click"
                                       Tag="{Binding ClaimId}"
                                       Margin="0,0,10,0"/>
                                <Button Style="{StaticResource DeleteButton}" 
                                       Content="Delete" 
                                       Click="DeleteClaim_Click"
                                       Tag="{Binding ClaimId}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
